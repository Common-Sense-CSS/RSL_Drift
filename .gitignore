# FXServer runtime artifacts — not part of the framework source
cache/
cache-priv/
txData/
server-data/
*.log

# FXServer binaries (downloaded separately, see README.md)
FXServer.exe
alt-v-server.exe
run.cmd
